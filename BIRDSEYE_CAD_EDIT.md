# FrisFrame 2.5D CAD 편집 흐름

2.5D 조감도는 별도 복제 데이터를 만들지 않고 기존 blocking state를 직접 편집합니다. 따라서 2D 마스터플랜, 2.5D 조감도, 3D 프리비즈가 같은 좌표와 잠금 상태를 공유합니다.

## CAD 보조 도구

2.5D 상단에는 기존 좌/우 조감, 전체보기, 지붕/천장 숨김, 아웃라인 아래에 CAD 보조 바가 표시됩니다.

- `자유`: X/Z 두 축 이동
- `X`: X축 이동만 허용하고 Z 위치를 드래그 시작점에 고정
- `Z`: Z축 이동만 허용하고 X 위치를 드래그 시작점에 고정
- `스냅`: 드래그 위치를 미터 그리드에 맞춤
- 스냅 간격: `0.10m`, `0.25m`, `0.50m`, `1.00m`
- `-15°`, `+15°`: 선택한 세트/대상을 15도 단위로 회전
- 좌표 읽기: 선택 대상의 X/Z 미터 좌표, 실제 크기, 잠금 상태 표시

## 키보드

2.5D에서 입력창을 편집 중이지 않을 때 다음 단축키를 사용할 수 있습니다.

- `X`: X축 고정 전환
- `Z`: Z축 고정 전환
- `Esc`: 자유 이동으로 복귀
- 방향키: 현재 스냅 간격만큼 미터 단위 이동
- `Shift + 방향키`: 4배 이동
- `Alt + 방향키`: 1/10 미세 이동
- `Q` / `E`: 15도씩 좌/우 회전

기존 타임라인 방향키와 충돌하지 않도록 2.5D일 때만 CAD 키 입력이 우선 처리됩니다.

## 잠금과 그룹

CAD 편집은 기존 `sourceEditLocked` 및 semantic set collection 잠금을 그대로 사용합니다. 잠긴 대상은 드래그, 미세 이동, 회전 모두 차단됩니다.

그룹 이동 중 축 고정이나 미터 스냅을 적용할 때는 `transformGroupItemIds`로 같은 그룹의 편집 가능한 멤버에 동일한 보정량을 적용합니다. 따라서 기존 그룹 변환과 2.5D 보조 기능이 분리되지 않습니다.

## 레퍼런스 → 2.5D 자동 연결

MCP live sync에서 원격 프로젝트 revision이 갱신되고 현재 컷의 `setMasterPlan` 내용이 실제로 변경되면 FrisFrame은 해당 변경을 불러온 직후 자동으로:

1. 블로킹 작업 화면으로 이동
2. `2.5D` 모드 전환
3. Orthographic `전체보기` 실행
4. 새 세트 마스터플랜이 2.5D로 열렸다는 상태 메시지 표시

다른 MCP 변경처럼 `setMasterPlan`이 바뀌지 않은 업데이트에는 자동 전환하지 않습니다. 사용자가 일반적으로 프로젝트를 여는 경우에도 이 동작은 발생하지 않습니다.
