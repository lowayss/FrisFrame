name: One-shot legacy export cleanup

on:
  push:
    branches:
      - refactor/mcp-first-previs-workflow

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/mcp-first-previs-workflow
          fetch-depth: 0
      - name: Remove retired shared export paths
        run: python3 tools/one_shot_retire_legacy_exports.py
      - name: Verify JavaScript syntax
        run: node --check app.js
      - name: Verify patch whitespace
        run: git diff --check
      - name: Commit cleanup
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- app.js index.html; then
            echo "No cleanup diff to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.js index.html
          git commit -m "Physically remove retired shared export paths"
          git push origin HEAD:refactor/mcp-first-previs-workflow
