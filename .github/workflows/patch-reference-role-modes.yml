name: Patch reference role modes

on:
  push:
    branches:
      - feature/clean-reference-prompt-guide-20260829

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Apply reference role modes
        run: python3 tools/patch_reference_role_modes.py
      - name: Run full quality checks
        run: python3 quality_check.py
      - name: Commit validated role modes
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add reference-workflow-core.js tests/reference-prompt-guide.test.cjs REFERENCE_PROMPT_GUIDE.md
          git commit -m "Distinguish previs and motion reference roles"
          git push
